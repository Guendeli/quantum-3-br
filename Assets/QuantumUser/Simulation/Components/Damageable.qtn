component Damageable
{
    FP Health;
    AssetRef<DamageableBase> DamageableData;
}

signal OnDamageableHit(EntityRef target, EntityRef source, FP damage, Damageable* damageable);

event OnDamageHit
{
     EntityRef entityRef;
     FP maxHealth;
     FP currentHealth;
}
